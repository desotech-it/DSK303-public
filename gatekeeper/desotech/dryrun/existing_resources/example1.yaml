apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-host1
spec:
  rules:
    - host: example.desotech.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx2
                port:
                  number: 80